[build]
  command = "npm ci && npm run build"
  publish = "dist"

# Proxy API to Render so relative `/api/*` fetches (e.g. Calendar) work from the static origin.
[[redirects]]
  from = "/api/*"
  to = "https://aibetter.onrender.com/api/:splat"
  status = 200
  force = true
